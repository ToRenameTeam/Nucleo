# frontend-service Helm values

```
  -f helm-values/app.values.yaml
helm upgrade --install frontend-service ../../kubernetes/helm-chart/node-chart \
```bash

## Example usage

- Application: `kubernetes/helm-chart/node-chart`

## Chart

This folder contains Helm overrides for deploying `frontend-service`.

