services:
  frontend-dev:
    build:
      context: ..
      dockerfile: frontend-service/Dockerfile.dev
    container_name: nucleo-frontend-dev
    ports:
      - '5173:5173'
    volumes:
      - ..:/app
      - /app/frontend-service/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
